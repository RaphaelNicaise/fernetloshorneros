{
  "name": "fernetloshorneros",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,md,yml,yaml}": "prettier --write",
    "frontend/**/*.{js,jsx,ts,tsx}": "pnpm -C frontend lint --fix",
    "backend/src/**/*.ts": "pnpm -C backend lint --fix"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "prettier-plugin-tailwindcss": "^0.8.0"
  }
}
